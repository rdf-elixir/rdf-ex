defmodule RDF.NormalizationTest do
  use RDF.Test.Case

  alias RDF.{NTriples, NQuads}


  test "the normalization of a RDF.Graph is equal to this RDF.Graph in a normalized RDF.Dataset" do

  end

  test "the normalization of a RDF.Description is equal to this RDF.Description in a normalized RDF.Dataset" do

  end
end
